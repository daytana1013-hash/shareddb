name: Deploy SharedDB to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 액세스 코드를 GitHub Secrets에서 주입
      # Settings → Secrets → SHAREDDB_ACCESS_CODE 로 등록하세요
      - name: Inject access code from Secret
        run: |
          sed -i "s/accessCode: 'db2024'/accessCode: '${{ secrets.SHAREDDB_ACCESS_CODE }}'/" js/config.js
          sed -i "s/showDemoHint: true/showDemoHint: false/" js/config.js

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
